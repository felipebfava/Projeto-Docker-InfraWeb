apiVersion: v1

kind: Service

# Por questões de Segurança
# O banco não poderá ser acessador externamente
# Apenas o backend poderá encontrá-lo

metadata:
  name: db-service

spec:
  type: ClusterIP

  selector:
    app: postgres

  ports:
    - port: 5432

      targetPort: 5432